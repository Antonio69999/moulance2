import { Metadata } from "next";

export const metadata: Metadata = {
  title: "View Expense",
  description: "View expense page",
};

export default function View() {
  return (
    <main>
      <h1>Voir mes dépenses</h1>
      {/* Further implementation for viewing expenses will go here */}
      {/* Example implementation can include a list of expenses */}
    </main>
  );
}
