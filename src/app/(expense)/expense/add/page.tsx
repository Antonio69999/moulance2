import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Add Expense",
  description: "Add expense page",
};

export default function Add() {
  return (
    <main>
      <h1>Add an Expense</h1>
      {/* Further implementation for adding expense will go here */}
    </main>
  );
}
